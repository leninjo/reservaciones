<div class="container-fluid alerta" id="alerta">
    <% flash.each do |name, msg| %>
      <div class="notification is-info is-light" role="alert">
          <%= msg %>   
      </div>
    <% end %>
</div>